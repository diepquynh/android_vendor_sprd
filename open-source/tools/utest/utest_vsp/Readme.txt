
Usage:

  utest_vsp_enc -i input_yuv -w width -h height -o output_bs -format format -framerate framerate -max_key_interval max_key_interval -bitrate bitrate -qp qp -frames frames


  utest_vsp_dec -i input_bs -o output_yuv -format format 



Auto Test: (Here's the example in SP8810EA)


shell@android:/ # utest_vspenc


