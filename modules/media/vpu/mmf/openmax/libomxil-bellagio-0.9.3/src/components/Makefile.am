if WITH_AUDIOEFFECTS
    MAYBE_AUDIO_EFFECTS = audio_effects
endif

if WITH_CLOCKSRC
    MAYBE_CLOCKSRC = clocksrc
endif

if WITH_VIDEOSCHEDULER
    MAYBE_VIDEOSCHEDULER = videoscheduler
endif

SUBDIRS = $(MAYBE_AUDIO_EFFECTS) $(MAYBE_CLOCKSRC) $(MAYBE_VIDEOSCHEDULER)
